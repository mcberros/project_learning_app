<%= simple_form_for @story, html: { class: 'form-horizontal' } do |f| %>
  <%= f.error_notification %>
  <%= f.association :project %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>

