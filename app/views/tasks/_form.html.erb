<%= simple_form_for @task, html: { class: 'form-horizontal' } do |f| %>
  <%= f.error_notification %>
  <%= f.association :story %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>

